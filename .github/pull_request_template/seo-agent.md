## SEO Agent PR

### Cadence

- [ ] Weekly / Monthly / Quarterly / Phase build

### Tasks executed

<!-- Auto-filled by orchestrator or agent -->

### QA report

- [ ] Titles follow national/geo ownership rules
- [ ] Local copy is genuinely local (geo pages)
- [ ] No pricing claims without approval
- [ ] Every changed page has primary CTA (/contact or quote)
- [ ] Case studies / strategy blogs read as authority content
- [ ] Registry diff matches work done (`pages.jsonl`, `rotation.json`)

### Revenue Model (monthly only)

<!-- demand-math.ts output -->

### CTA audit (monthly only)

<!-- conversion.cta_audit output -->

### Preview URLs (append paths — Vercel Visit opens `/` only)

After the Preview deploy is ready:

- Index: `https://lcprefresh-git-development-latestcrazeproductions-projects.vercel.app/pages`
- Changed pages: preview-host + path(s) from this PR (e.g. `/blog/{slug}`, `/work/{slug}`, `/resources/{slug}`)

If the preview shows **Vercel Login**, disable Preview Deployment Protection (see `docs/DEPLOY_VERCEL.md`).

### Reviewer

- Marketing merge approval: ___________
- Preview checked (content path, not just `/`): ___________
