# Copy to .env.development.local for local work on git branch `development`.
# Next.js loads this automatically when NODE_ENV=development (npm run dev).
#
# Production Supabase (main / live site) — keep in Vercel Production env only:
#   NEXT_PUBLIC_SUPABASE_URL=https://qsccsddknmvidvcfpffu.supabase.co
#
# Development Supabase project: LCP Development (aafiingdoulyghucrzat)

NEXT_PUBLIC_SUPABASE_URL=https://aafiingdoulyghucrzat.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_danPW6_6Ry-iSmlJjNNrCA_OzFP3EzO

# Server-only — copy from Supabase Dashboard → LCP Development → Settings → API → service_role
# SUPABASE_SERVICE_ROLE_KEY=your-dev-service-role-key

# Canonical site URL for OG/sitemap XML. HTML sitemap links use the request host in dev.
# If port 3000 is taken, run `npm run dev:3002` and set this to match.
NEXT_PUBLIC_SITE_URL=http://localhost:3002
